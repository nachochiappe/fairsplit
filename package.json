{
  "name": "fairsplit",
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "prebuild": "pnpm --filter @fairsplit/db prisma:generate",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e:agentmail:onboarding": "pnpm --filter @fairsplit/api exec tsx ../../scripts/e2e-agentmail-onboarding.ts",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "db:generate": "pnpm --filter @fairsplit/db prisma:generate",
    "db:new-migration": "./scripts/migrations.sh new",
    "db:verify": "./scripts/migrations.sh verify",
    "db:reset-test": "./scripts/migrations.sh reset-test"
  },
  "devDependencies": {
    "prettier": "^3.5.1",
    "typescript": "^5.7.3"
  }
}
