{
  "name": "@fairsplit/shared",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "echo 'No dev server for @fairsplit/shared'",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo 'No lint step for @fairsplit/shared'"
  },
  "dependencies": {
    "decimal.js": "^10.5.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
