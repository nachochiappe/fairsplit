{
  "name": "@fairsplit/db",
  "version": "0.0.1",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "dev": "echo 'No dev server for @fairsplit/db'",
    "build": "pnpm prisma:generate && tsc -p tsconfig.json",
    "test": "echo 'No tests for @fairsplit/db'",
    "lint": "echo 'No lint step for @fairsplit/db'",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "prisma": "^6.19.3",
    "tsx": "^4.19.2"
  }
}
