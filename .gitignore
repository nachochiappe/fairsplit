# Dependencies and package managers
node_modules
.pnpm-store

# Build and test outputs
.next
dist
coverage
*.tsbuildinfo
apps/api/test-results

# Environment and local config
.env
.env.*
!.env.example

# Database and local data
packages/db/data
# supabase/ is tracked (config.toml, migrations); its local CLI state is
# excluded by supabase/.gitignore.

# Generated docs and automation artifacts
docs/
.playwright-cli

# System and local tooling
.DS_Store
.agents
